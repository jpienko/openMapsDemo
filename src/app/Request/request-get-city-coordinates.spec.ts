import { RequestGetCityCoordinates } from './request-get-city-coordinates';

describe('RequestGetCityCoordinates', () => {
  it('should create an instance', () => {
    expect(new RequestGetCityCoordinates()).toBeTruthy();
  });
});
